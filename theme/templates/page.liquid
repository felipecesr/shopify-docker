<!-- /templates/page.liquid -->
<section class="page">
  <h1 class="page-title standard-single">{{ page.title }}</h1>
  
  <div class="grid">
    <div class="grid__item large--two-thirds push--large--one-sixth page-item">
      {% comment %}
        Regular page content goes here.
      {% endcomment %}
      <div class="rte feature-copy">
        {{ page.content }}
      </div>
    </div>
  
  </div>
</section>